import React from 'react';
import NavigationLink from "../../router/NavigationLink";

const Header=(props)=>(
    <header>
        <h2>Expensify</h2>
        <NavigationLink/>
    </header>
)

export default Header;